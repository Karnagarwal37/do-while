#include<stdio.h>
int main(){
    for(int i=1;i<5;i++){
        if(i==3) {
            break;
        }
        printf("%d\n",i);
        
        }
        printf("end ");
        return 0;
}
#include<stdio.h>
int main(){
    
   printf("enter the no:");
   scanf("%d",&n);
   printf("%d\n",n);
   if(n % 2!=0){
       break;
   }
}while(1);
printf("thank you");
